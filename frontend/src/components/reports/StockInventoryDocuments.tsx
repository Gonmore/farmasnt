import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, Tooltip, XAxis, YAxis } from 'recharts'
import { ExportLegend } from './ExportLegend'
import { getChartColor } from './chartTheme'

function barColors(idx: number): string {
  return getChartColor(idx, 'rainbow')
}

function qty(n: number): string {
  if (!Number.isFinite(n)) return '0'
  return n.toFixed(0)
}

type ShellProps = {
  title: string
  from: string
  to: string
  children: React.ReactNode
}

function Shell({ title, from, to, children }: ShellProps) {
  return (
    <div className="mx-auto w-[1240px] bg-white px-10 py-8 text-slate-900">
      <div className="mb-6 rounded-2xl border border-slate-200 bg-slate-50 px-6 py-5">
        <div className="text-2xl font-bold tracking-tight">{title}</div>
        <div className="mt-2 text-sm text-slate-600">Periodo: {from} a {to}</div>
      </div>
      {children}
    </div>
  )
}

export type StockInputsSummaryItem = { sku: string; name: string; quantity: number; movementsCount: number }
export type StockInputsWarehouseSection = { warehouse: string; totalQuantity: number; items: Array<{ sku: string; name: string; quantity: number; movementsCount: number }> }

export function StockInputsDocument({ title, from, to, items, warehouses }: { title: string; from: string; to: string; items: StockInputsSummaryItem[]; warehouses: StockInputsWarehouseSection[] }) {
  const totalQuantity = items.reduce((sum, item) => sum + item.quantity, 0)
  const totalMovements = items.reduce((sum, item) => sum + item.movementsCount, 0)
  return <Shell title={title} from={from} to={to}><div className="mb-8 grid grid-cols-4 gap-4"><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Productos</div><div className="mt-2 text-3xl font-bold">{items.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Movimientos IN</div><div className="mt-2 text-3xl font-bold">{totalMovements}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Unidades</div><div className="mt-2 text-3xl font-bold text-emerald-700">{qty(totalQuantity)}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Sucursales</div><div className="mt-2 text-3xl font-bold">{warehouses.length}</div></div></div><div className="mb-8 grid grid-cols-[520px_minmax(0,1fr)] gap-6"><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Distribucion por sucursal</div><div className="flex justify-center"><PieChart width={460} height={320}><Pie data={warehouses.map((section) => ({ name: section.warehouse, value: section.totalQuantity }))} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={110} label={({ name, percent }) => `${name} ${((percent ?? 0) * 100).toFixed(0)}%`} isAnimationActive={false}>{warehouses.map((_, idx) => <Cell key={idx} fill={barColors(idx)} />)}</Pie><Tooltip formatter={(value: number | string | undefined) => [qty(Number(value ?? 0)), 'Unidades']} /></PieChart></div><ExportLegend items={warehouses.map((section, idx) => ({ label: section.warehouse, color: barColors(idx) }))} className="mt-3 grid grid-cols-2 gap-x-4 gap-y-2 text-sm text-slate-700" /></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Ranking de ingresos</div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[14%] px-3 py-2">SKU</th><th className="px-3 py-2">Producto</th><th className="w-[18%] px-3 py-2 text-right">Mov.</th><th className="w-[18%] px-3 py-2 text-right">Unidades</th></tr></thead><tbody>{items.map((item) => <tr key={item.sku} className="border-b border-slate-100"><td className="px-3 py-3 font-mono text-xs">{item.sku}</td><td className="px-3 py-3">{item.name}</td><td className="px-3 py-3 text-right tabular-nums">{item.movementsCount}</td><td className="px-3 py-3 text-right font-semibold text-emerald-700 tabular-nums">{qty(item.quantity)}</td></tr>)}</tbody></table></div></div><div className="text-lg font-bold tracking-tight">Detalle por sucursal</div><div className="mt-4 space-y-6">{warehouses.map((section) => <section key={section.warehouse} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><div className="text-base font-semibold">{section.warehouse}</div><div className="mt-1 text-sm text-slate-600">{section.items.length} productos · {qty(section.totalQuantity)} unidades</div></div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-white text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[14%] px-4 py-3">SKU</th><th className="px-4 py-3">Producto</th><th className="w-[18%] px-4 py-3 text-right">Movimientos</th><th className="w-[18%] px-4 py-3 text-right">Unidades</th></tr></thead><tbody>{section.items.map((item) => <tr key={`${section.warehouse}-${item.sku}`} className="border-b border-slate-100"><td className="px-4 py-3 font-mono text-xs">{item.sku}</td><td className="px-4 py-3">{item.name}</td><td className="px-4 py-3 text-right tabular-nums">{item.movementsCount}</td><td className="px-4 py-3 text-right font-semibold text-emerald-700 tabular-nums">{qty(item.quantity)}</td></tr>)}</tbody></table></section>)}</div></Shell>
}

export type StockTransfersRoute = { origin: string; destination: string; quantity: number; movementsCount: number }
export function StockTransfersDocument({ title, from, to, routes }: { title: string; from: string; to: string; routes: StockTransfersRoute[] }) {
  const totalQuantity = routes.reduce((sum, item) => sum + item.quantity, 0)
  const totalMovements = routes.reduce((sum, item) => sum + item.movementsCount, 0)
  return <Shell title={title} from={from} to={to}><div className="mb-8 grid grid-cols-3 gap-4"><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Rutas</div><div className="mt-2 text-3xl font-bold">{routes.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Movimientos</div><div className="mt-2 text-3xl font-bold">{totalMovements}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Unidades transferidas</div><div className="mt-2 text-3xl font-bold text-amber-700">{qty(totalQuantity)}</div></div></div><div className="mb-8 rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Top rutas</div><div className="flex justify-center"><BarChart width={1120} height={340} data={routes.slice(0, 12).map((item) => ({ route: `${item.origin} → ${item.destination}`.length > 40 ? `${item.origin} → ${item.destination}`.slice(0, 40) + '…' : `${item.origin} → ${item.destination}`, quantity: item.quantity, movementsCount: item.movementsCount }))} margin={{ top: 10, right: 20, left: 10, bottom: 80 }}><CartesianGrid stroke="#e2e8f0" strokeDasharray="3 3" /><XAxis dataKey="route" angle={-35} textAnchor="end" interval={0} height={90} tick={{ fontSize: 11, fill: '#475569' }} /><YAxis tick={{ fontSize: 11, fill: '#475569' }} /><Tooltip /><Bar dataKey="quantity" name="Unidades" fill="#f59e0b" radius={[6,6,0,0]} isAnimationActive={false} /><Bar dataKey="movementsCount" name="Movimientos" fill="#2563eb" radius={[6,6,0,0]} isAnimationActive={false} /></BarChart></div><ExportLegend items={[{ label: 'Unidades', color: '#f59e0b' }, { label: 'Movimientos', color: '#2563eb' }]} /></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Detalle de rutas</div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[35%] px-3 py-2">Origen</th><th className="w-[35%] px-3 py-2">Destino</th><th className="w-[15%] px-3 py-2 text-right">Mov.</th><th className="w-[15%] px-3 py-2 text-right">Unid.</th></tr></thead><tbody>{routes.map((item) => <tr key={`${item.origin}-${item.destination}`} className="border-b border-slate-100"><td className="px-3 py-3">{item.origin}</td><td className="px-3 py-3">{item.destination}</td><td className="px-3 py-3 text-right tabular-nums">{item.movementsCount}</td><td className="px-3 py-3 text-right font-semibold text-amber-700 tabular-nums">{qty(item.quantity)}</td></tr>)}</tbody></table></div></Shell>
}

export type StockRotationItem = { productId: string; sku: string; name: string; movementsIn: number; movementsOut: number; totalMovements: number; currentStock: number; warehouse?: string }
export type StockRotationWarehouseSection = { warehouse: string; items: StockRotationItem[] }
export function StockRotationDocument({ title, from, to, items, warehouses, mode }: { title: string; from: string; to: string; items: StockRotationItem[]; warehouses: StockRotationWarehouseSection[]; mode: 'ROTATION' | 'NOMOVEMENT' }) {
  const avg = items.length > 0 ? items.reduce((sum, item) => sum + item.totalMovements, 0) / items.length : 0
  const shownItems = mode === 'NOMOVEMENT' ? items.filter((item) => item.totalMovements <= 1) : items
  return <Shell title={title} from={from} to={to}><div className="mb-8 grid grid-cols-4 gap-4"><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Productos</div><div className="mt-2 text-3xl font-bold">{shownItems.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Movimientos promedio</div><div className="mt-2 text-3xl font-bold">{avg.toFixed(1)}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Alta rotacion</div><div className="mt-2 text-3xl font-bold text-emerald-700">{shownItems.filter((item) => item.totalMovements > avg).length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Sucursales</div><div className="mt-2 text-3xl font-bold">{warehouses.length}</div></div></div><div className="mb-8 rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Productos principales</div><div className="flex justify-center"><BarChart width={1120} height={340} data={shownItems.slice(0, 12).map((item) => ({ name: item.name.length > 18 ? `${item.name.slice(0,18)}…` : item.name, totalMovements: item.totalMovements, currentStock: item.currentStock }))} margin={{ top: 10, right: 20, left: 10, bottom: 70 }}><CartesianGrid stroke="#e2e8f0" strokeDasharray="3 3" /><XAxis dataKey="name" angle={-35} textAnchor="end" interval={0} height={80} tick={{ fontSize: 11, fill: '#475569' }} /><YAxis tick={{ fontSize: 11, fill: '#475569' }} /><Tooltip /><Bar dataKey="totalMovements" name="Movimientos" fill="#2563eb" radius={[6,6,0,0]} isAnimationActive={false} /><Bar dataKey="currentStock" name="Stock actual" fill="#10b981" radius={[6,6,0,0]} isAnimationActive={false} /></BarChart></div><ExportLegend items={[{ label: 'Movimientos', color: '#2563eb' }, { label: 'Stock actual', color: '#10b981' }]} /></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Detalle consolidado</div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[12%] px-3 py-2">SKU</th><th className="px-3 py-2">Producto</th><th className="w-[12%] px-3 py-2 text-right">Entradas</th><th className="w-[12%] px-3 py-2 text-right">Salidas</th><th className="w-[12%] px-3 py-2 text-right">Mov.</th><th className="w-[12%] px-3 py-2 text-right">Stock</th></tr></thead><tbody>{shownItems.map((item) => <tr key={`${item.warehouse ?? 'all'}-${item.productId}`} className="border-b border-slate-100"><td className="px-3 py-3 font-mono text-xs">{item.sku}</td><td className="px-3 py-3">{item.name}</td><td className="px-3 py-3 text-right tabular-nums">{item.movementsIn}</td><td className="px-3 py-3 text-right tabular-nums">{item.movementsOut}</td><td className="px-3 py-3 text-right tabular-nums">{item.totalMovements}</td><td className="px-3 py-3 text-right font-semibold text-emerald-700 tabular-nums">{qty(item.currentStock)}</td></tr>)}</tbody></table></div><div className="mt-6 text-lg font-bold tracking-tight">Detalle por sucursal</div><div className="mt-4 space-y-6">{warehouses.map((section) => <section key={section.warehouse} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><div className="text-base font-semibold">{section.warehouse}</div><div className="mt-1 text-sm text-slate-600">{section.items.length} productos</div></div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-white text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[12%] px-4 py-3">SKU</th><th className="px-4 py-3">Producto</th><th className="w-[14%] px-4 py-3 text-right">Entradas</th><th className="w-[14%] px-4 py-3 text-right">Salidas</th><th className="w-[14%] px-4 py-3 text-right">Mov.</th><th className="w-[14%] px-4 py-3 text-right">Stock</th></tr></thead><tbody>{section.items.filter((item) => mode === 'ROTATION' || item.totalMovements <= 1).map((item) => <tr key={`${section.warehouse}-${item.productId}`} className="border-b border-slate-100"><td className="px-4 py-3 font-mono text-xs">{item.sku}</td><td className="px-4 py-3">{item.name}</td><td className="px-4 py-3 text-right tabular-nums">{item.movementsIn}</td><td className="px-4 py-3 text-right tabular-nums">{item.movementsOut}</td><td className="px-4 py-3 text-right tabular-nums">{item.totalMovements}</td><td className="px-4 py-3 text-right font-semibold text-emerald-700 tabular-nums">{qty(item.currentStock)}</td></tr>)}</tbody></table></section>)}</div></Shell>
}

export type StockLowStockItem = { productId: string; sku: string; name: string; currentStock: number; minStock: number; avgDailySales: number; daysOfStock: number | null }
export type StockLowStockWarehouseSection = { warehouse: string; items: Array<{ productId: string; sku: string; name: string; currentStock: number }> }
export function StockLowStockDocument({ title, from, to, items, warehouses }: { title: string; from: string; to: string; items: StockLowStockItem[]; warehouses: StockLowStockWarehouseSection[] }) {
  const critical = items.filter((item) => item.currentStock <= 5)
  const low = items.filter((item) => item.currentStock > 5 && item.currentStock <= 10)
  return <Shell title={title} from={from} to={to}><div className="mb-8 grid grid-cols-4 gap-4"><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Critico</div><div className="mt-2 text-3xl font-bold text-rose-700">{critical.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Bajo</div><div className="mt-2 text-3xl font-bold text-amber-700">{low.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Alertas</div><div className="mt-2 text-3xl font-bold">{items.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Sucursales</div><div className="mt-2 text-3xl font-bold">{warehouses.length}</div></div></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Detalle consolidado</div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[12%] px-3 py-2">SKU</th><th className="px-3 py-2">Producto</th><th className="w-[12%] px-3 py-2 text-right">Actual</th><th className="w-[12%] px-3 py-2 text-right">Min.</th><th className="w-[12%] px-3 py-2 text-right">Dias</th><th className="w-[16%] px-3 py-2 text-right">Venta diaria</th></tr></thead><tbody>{items.map((item) => <tr key={item.productId} className="border-b border-slate-100"><td className="px-3 py-3 font-mono text-xs">{item.sku}</td><td className="px-3 py-3">{item.name}</td><td className={`px-3 py-3 text-right font-semibold tabular-nums ${item.currentStock <= 5 ? 'text-rose-700' : 'text-amber-700'}`}>{qty(item.currentStock)}</td><td className="px-3 py-3 text-right tabular-nums">{qty(item.minStock)}</td><td className="px-3 py-3 text-right tabular-nums">{item.daysOfStock === null ? '—' : item.daysOfStock.toFixed(0)}</td><td className="px-3 py-3 text-right tabular-nums">{item.avgDailySales.toFixed(2)}</td></tr>)}</tbody></table></div><div className="mt-6 text-lg font-bold tracking-tight">Cobertura por sucursal</div><div className="mt-4 space-y-6">{warehouses.map((section) => <section key={section.warehouse} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><div className="text-base font-semibold">{section.warehouse}</div><div className="mt-1 text-sm text-slate-600">{section.items.length} productos comprometidos</div></div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-white text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[14%] px-4 py-3">SKU</th><th className="px-4 py-3">Producto</th><th className="w-[18%] px-4 py-3 text-right">Stock local</th></tr></thead><tbody>{section.items.map((item) => <tr key={`${section.warehouse}-${item.productId}`} className="border-b border-slate-100"><td className="px-4 py-3 font-mono text-xs">{item.sku}</td><td className="px-4 py-3">{item.name}</td><td className="px-4 py-3 text-right font-semibold text-rose-700 tabular-nums">{qty(item.currentStock)}</td></tr>)}</tbody></table></section>)}</div></Shell>
}

export type StockExpiryItem = { productId: string; sku: string; name: string; warehouse: string; locationName: string; lotNumber: string | null; expiryDate: string | null; quantity: number; daysUntilExpiry: number | null }
export type StockExpiryWarehouseSection = { warehouse: string; items: StockExpiryItem[] }
export function StockExpiryDocument({ title, from, to, items, warehouses }: { title: string; from: string; to: string; items: StockExpiryItem[]; warehouses: StockExpiryWarehouseSection[] }) {
  const expired = items.filter((item) => (item.daysUntilExpiry ?? 9999) < 0)
  const week = items.filter((item) => (item.daysUntilExpiry ?? 9999) >= 0 && (item.daysUntilExpiry ?? 9999) <= 7)
  const month = items.filter((item) => (item.daysUntilExpiry ?? 9999) > 7 && (item.daysUntilExpiry ?? 9999) <= 30)
  return <Shell title={title} from={from} to={to}><div className="mb-8 grid grid-cols-4 gap-4"><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Vencidos</div><div className="mt-2 text-3xl font-bold text-rose-700">{expired.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Esta semana</div><div className="mt-2 text-3xl font-bold text-orange-700">{week.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Este mes</div><div className="mt-2 text-3xl font-bold text-amber-700">{month.length}</div></div><div className="rounded-xl border border-slate-200 bg-white px-4 py-4"><div className="text-xs font-semibold uppercase tracking-wider text-slate-500">Sucursales</div><div className="mt-2 text-3xl font-bold">{warehouses.length}</div></div></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-5"><div className="mb-4 text-sm font-semibold uppercase tracking-wider text-slate-600">Detalle consolidado</div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[12%] px-3 py-2">SKU</th><th className="px-3 py-2">Producto</th><th className="w-[18%] px-3 py-2">Sucursal</th><th className="w-[14%] px-3 py-2">Ubicacion</th><th className="w-[12%] px-3 py-2">Lote</th><th className="w-[12%] px-3 py-2 text-right">Unid.</th><th className="w-[12%] px-3 py-2 text-right">Dias</th></tr></thead><tbody>{items.map((item, idx) => <tr key={`${item.productId}-${idx}`} className="border-b border-slate-100"><td className="px-3 py-3 font-mono text-xs">{item.sku}</td><td className="px-3 py-3">{item.name}</td><td className="px-3 py-3">{item.warehouse}</td><td className="px-3 py-3">{item.locationName}</td><td className="px-3 py-3">{item.lotNumber ?? '—'}</td><td className="px-3 py-3 text-right tabular-nums">{qty(item.quantity)}</td><td className={`px-3 py-3 text-right tabular-nums ${(item.daysUntilExpiry ?? 9999) < 0 ? 'text-rose-700' : (item.daysUntilExpiry ?? 9999) <= 7 ? 'text-orange-700' : 'text-amber-700'}`}>{item.daysUntilExpiry ?? '—'}</td></tr>)}</tbody></table></div><div className="mt-6 text-lg font-bold tracking-tight">Detalle por sucursal</div><div className="mt-4 space-y-6">{warehouses.map((section) => <section key={section.warehouse} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-200 bg-slate-50 px-5 py-4"><div className="text-base font-semibold">{section.warehouse}</div><div className="mt-1 text-sm text-slate-600">{section.items.length} lotes comprometidos</div></div><table className="w-full table-fixed border-collapse text-sm"><thead><tr className="border-b border-slate-200 bg-white text-left text-xs uppercase tracking-wider text-slate-500"><th className="w-[12%] px-4 py-3">SKU</th><th className="px-4 py-3">Producto</th><th className="w-[16%] px-4 py-3">Ubicacion</th><th className="w-[12%] px-4 py-3">Lote</th><th className="w-[14%] px-4 py-3">Vence</th><th className="w-[10%] px-4 py-3 text-right">Unid.</th><th className="w-[10%] px-4 py-3 text-right">Dias</th></tr></thead><tbody>{section.items.map((item, idx) => <tr key={`${section.warehouse}-${item.productId}-${idx}`} className="border-b border-slate-100"><td className="px-4 py-3 font-mono text-xs">{item.sku}</td><td className="px-4 py-3">{item.name}</td><td className="px-4 py-3">{item.locationName}</td><td className="px-4 py-3">{item.lotNumber ?? '—'}</td><td className="px-4 py-3">{item.expiryDate ?? '—'}</td><td className="px-4 py-3 text-right tabular-nums">{qty(item.quantity)}</td><td className="px-4 py-3 text-right tabular-nums">{item.daysUntilExpiry ?? '—'}</td></tr>)}</tbody></table></section>)}</div></Shell>
}